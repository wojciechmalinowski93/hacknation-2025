{% load i18n %}
---
tags:
  - API Endpoints
summary: Returns list of histories
description: |
  {% spaceless %}
      <p>Gives the ability to browse, filter and search histories.</p>
      {% include "docs/histories/obj_description.html" %}
  {% endspaceless %}
parameters:
  schema: LogEntryApiSearchRequest
responses:
  200:
    description: valid response in jsonapi format
    content:
      application/vnd.api+json:
        schema:
          $ref: '#/components/schemas/Histories'
