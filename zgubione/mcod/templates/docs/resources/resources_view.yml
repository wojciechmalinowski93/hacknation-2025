{% load i18n %}
---
tags:
  - API Endpoints
summary: Returns list of resources
description: |
  {% spaceless %}
      <p>Gives the ability to browse, filter and search resources.</p>
      {% include "docs/resources/obj_description.html" %}
  {% endspaceless %}
parameters:
  schema: ResourceApiSearchRequest
responses:
  200:
    description: valid response in jsonapi format
    content:
      application/vnd.api+json:
        schema:
          $ref: '#/components/schemas/Resources'
