{% load i18n %}
---
tags:
   - {% trans "API Endpoints" %}
summary: {% trans "Get single resource" %}
description: |
  {% spaceless %}
      <p>Returns resource with given ID.</p>
      {% include "docs/resources/obj_description.html" %}
  {% endspaceless %}
parameters:
    schema: ResourceApiRequest
responses:
    200:
        description: valid response in jsonapi format
        content:
            application/vnd.api+json:
                schema:
                    $ref: '#/components/schemas/Resource'
